version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: UNIAdaptiveLTI-Back
    container_name: UNIAdaptiveLTI-Back
    restart: unless-stopped
    tty: true
    ports:
      - 8000:8000
    environment:
      APP_NAME: UNIAdaptiveLTI-BackEnd
      ETC:
    working_dir: /var/www/html
    volumes:
      - ./:/var/www/html
      - ./docker/php/local.ini:/usr/local/etc/php/conf.d/local.ini
    networks:
      - uniadaptive-network
networks:
  uniadaptive-network:
    driver: bridge